# AI VTuber Dependencies
# Install with: pip install -r requirements.txt
# 
# Platform Support:
#   - Windows 10/11 (native)
#   - Linux (Ubuntu, Debian, etc.)
#   - WSL (Windows Subsystem for Linux)
#   - macOS (experimental)

# Core
pyyaml>=6.0
tzdata>=2024.1  # IANA tz database (required for zoneinfo on Windows)

ruamel.yaml>=0.18.0
openai>=1.0.0
numpy>=1.24.0
Pillow>=10.0.0  # Image processing for screen capture and vision

# Audio
sounddevice>=0.4.6  # Audio playback and microphone input
soundfile>=0.12.0   # Optional: for audio file handling
webrtcvad>=2.0.10

# STT (faster-whisper uses CTranslate2, no PyTorch needed)
faster-whisper>=1.0.0

# TTS - KittenTTS
# Official 0.8.x from GitHub (recommended):
# pip install https://github.com/KittenML/KittenTTS/releases/download/0.8.1/kittentts-0.8.1-py3-none-any.whl
#
# OR PyPI 0.1.x (fallback):
kittentts>=0.1.0

# Avatar (Live2D)
live2d-py>=0.7.0

# UI
PyOpenGL>=3.1.0
PySide6>=6.6.0

# Screen Vision (for desktop/game watching)
mss>=9.0.0  # Fast cross-platform screen capture

# Optional: CUDA support for faster-whisper (CTranslate2)
# If you have CUDA, install ctranslate2 with CUDA support:
# pip install ctranslate2  # CUDA version included by default on supported systems
#
# Windows CUDA users: Make sure NVIDIA CUDA Toolkit is installed
# Download from: https://developer.nvidia.com/cuda-downloads
